{{!-- testing handlebars by way of hello world --}}
<p class="paragraph">hello Handlebars.js world</p>

<ul>
  {{#each tickets}}
    <p>{{this.id}}</p>
    <p>{{this.ticket_name}}</p>
    <p>If you see a "0", then that means false: <span>{{this.completed}}</span></p>
  {{/each}}
</ul>